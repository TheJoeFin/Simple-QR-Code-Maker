<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Simple_QR_Code_Maker.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Simple_QR_Code_Maker.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Simple_QR_Code_Maker.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" IsBackButtonVisible="{x:Bind ViewModel.IsBackVisible, Mode=OneWay}">
            <TitleBar.Content>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="160" />
                    </Grid.ColumnDefinitions>
                    <AutoSuggestBox
                        x:Name="TitleBarSearchBox"
                        MinWidth="320"
                        MaxWidth="800"
                        ItemsSource="{x:Bind ViewModel.TitleBarSearchResults,
                                             Mode=OneWay}"
                        PlaceholderText="Search FAQs, history, and brands"
                        QueryIcon="Find"
                        QuerySubmitted="TitleBarSearchBox_QuerySubmitted"
                        SuggestionChosen="TitleBarSearchBox_SuggestionChosen"
                        TextChanged="TitleBarSearchBox_TextChanged">
                        <AutoSuggestBox.ItemTemplate>
                            <DataTemplate x:DataType="models:TitleBarSearchResult">
                                <StackPanel Padding="8,4">
                                    <TextBlock
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind KindLabel,
                                                      Mode=OneTime}" />
                                    <TextBlock
                                        Style="{StaticResource BodyStrongTextBlockStyle}"
                                        Text="{x:Bind Title,
                                                      Mode=OneTime}"
                                        TextTrimming="CharacterEllipsis" />
                                    <TextBlock
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Text="{x:Bind Subtitle,
                                                      Mode=OneTime}"
                                        TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </DataTemplate>
                        </AutoSuggestBox.ItemTemplate>
                    </AutoSuggestBox>
                </Grid>
            </TitleBar.Content>
            <TitleBar.IconSource>
                <BitmapIconSource ShowAsMonochrome="False" UriSource="ms-appx:///Assets/WindowIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>
        <Frame x:Name="NavigationFrame" Grid.Row="1" />
    </Grid>
</Page>
