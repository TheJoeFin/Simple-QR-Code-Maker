<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="Simple_QR_Code_Maker.Controls.VCardBuilderDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Build vCard"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="{x:Bind ViewModel.CanApply, Mode=OneWay}"
    PrimaryButtonText="Apply"
    mc:Ignorable="d">

    <ScrollViewer
        x:Name="DialogScrollViewer"
        MaxHeight="680"
        HorizontalScrollBarVisibility="Disabled"
        HorizontalScrollMode="Disabled"
        VerticalScrollBarVisibility="Auto">
        <Border
            Width="{x:Bind DialogScrollViewer.ActualWidth, Mode=OneWay}"
            MinWidth="460"
            Padding="0,0,12,0">
            <StackPanel HorizontalAlignment="Stretch" Spacing="12">
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Start with the common contact fields, then use + to add optional details." />

                <TextBox
                    x:Name="FormattedNameTextBox"
                    Header="Formatted name"
                    PlaceholderText="Jane Doe"
                    Text="{x:Bind ViewModel.FormattedName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        Grid.Column="0"
                        Header="First name"
                        PlaceholderText="Jane"
                        Text="{x:Bind ViewModel.FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        Grid.Column="1"
                        Header="Last name"
                        PlaceholderText="Doe"
                        Text="{x:Bind ViewModel.LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>

                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        Grid.Column="0"
                        Header="Middle"
                        Text="{x:Bind ViewModel.MiddleName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        Grid.Column="1"
                        Header="Prefix"
                        PlaceholderText="Dr."
                        Text="{x:Bind ViewModel.Prefix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        Grid.Column="2"
                        Header="Suffix"
                        PlaceholderText="Jr."
                        Text="{x:Bind ViewModel.Suffix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>

                <TextBox
                    Header="Organization"
                    PlaceholderText="Contoso"
                    Text="{x:Bind ViewModel.Organization, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBox
                    Header="Title"
                    PlaceholderText="Product Manager"
                    Text="{x:Bind ViewModel.JobTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBox
                    Header="Mobile phone"
                    PlaceholderText="+1 555 0100"
                    Text="{x:Bind ViewModel.MobilePhone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBox
                    Header="Email"
                    PlaceholderText="jane@example.com"
                    Text="{x:Bind ViewModel.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBox
                    Header="Website"
                    PlaceholderText="https://example.com"
                    Text="{x:Bind ViewModel.Url, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Button
                    HorizontalAlignment="Left"
                    ToolTipService.ToolTip="Add an optional vCard field">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <SymbolIcon Symbol="Add" />
                        <TextBlock Text="Add field" />
                    </StackPanel>
                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Command="{x:Bind ViewModel.AddOptionalFieldCommand}" CommandParameter="WorkPhone" Text="Work phone" />
                            <MenuFlyoutItem Command="{x:Bind ViewModel.AddOptionalFieldCommand}" CommandParameter="HomePhone" Text="Home phone" />
                            <MenuFlyoutItem Command="{x:Bind ViewModel.AddOptionalFieldCommand}" CommandParameter="Nickname" Text="Nickname" />
                            <MenuFlyoutItem Command="{x:Bind ViewModel.AddOptionalFieldCommand}" CommandParameter="Birthday" Text="Birthday" />
                            <MenuFlyoutItem Command="{x:Bind ViewModel.AddOptionalFieldCommand}" CommandParameter="Address" Text="Address" />
                            <MenuFlyoutItem Command="{x:Bind ViewModel.AddOptionalFieldCommand}" CommandParameter="Note" Text="Note" />
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>

                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Visibility="{x:Bind ViewModel.WorkPhoneVisibility, Mode=OneWay}">
                    <StackPanel Spacing="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Text="Work phone" />
                            <HyperlinkButton
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                Command="{x:Bind ViewModel.RemoveOptionalFieldCommand}"
                                CommandParameter="WorkPhone"
                                ToolTipService.ToolTip="Remove work phone">
                                <SymbolIcon Symbol="Delete" />
                            </HyperlinkButton>
                        </Grid>
                        <TextBox
                            PlaceholderText="+1 555 0101"
                            Text="{x:Bind ViewModel.WorkPhone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Border>

                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Visibility="{x:Bind ViewModel.HomePhoneVisibility, Mode=OneWay}">
                    <StackPanel Spacing="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Text="Home phone" />
                            <HyperlinkButton
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                Command="{x:Bind ViewModel.RemoveOptionalFieldCommand}"
                                CommandParameter="HomePhone"
                                ToolTipService.ToolTip="Remove home phone">
                                <SymbolIcon Symbol="Delete" />
                            </HyperlinkButton>
                        </Grid>
                        <TextBox
                            PlaceholderText="+1 555 0102"
                            Text="{x:Bind ViewModel.HomePhone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Border>

                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Visibility="{x:Bind ViewModel.NicknameVisibility, Mode=OneWay}">
                    <StackPanel Spacing="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Text="Nickname" />
                            <HyperlinkButton
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                Command="{x:Bind ViewModel.RemoveOptionalFieldCommand}"
                                CommandParameter="Nickname"
                                ToolTipService.ToolTip="Remove nickname">
                                <SymbolIcon Symbol="Delete" />
                            </HyperlinkButton>
                        </Grid>
                        <TextBox
                            PlaceholderText="Janie"
                            Text="{x:Bind ViewModel.Nickname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Border>

                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Visibility="{x:Bind ViewModel.BirthdayVisibility, Mode=OneWay}">
                    <StackPanel Spacing="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Text="Birthday" />
                            <HyperlinkButton
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                Command="{x:Bind ViewModel.RemoveOptionalFieldCommand}"
                                CommandParameter="Birthday"
                                ToolTipService.ToolTip="Remove birthday">
                                <SymbolIcon Symbol="Delete" />
                            </HyperlinkButton>
                        </Grid>
                        <TextBox
                            PlaceholderText="1990-10-21"
                            Text="{x:Bind ViewModel.Birthday, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Border>

                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Visibility="{x:Bind ViewModel.AddressVisibility, Mode=OneWay}">
                    <StackPanel Spacing="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Text="Address" />
                            <HyperlinkButton
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                Command="{x:Bind ViewModel.RemoveOptionalFieldCommand}"
                                CommandParameter="Address"
                                ToolTipService.ToolTip="Remove address">
                                <SymbolIcon Symbol="Delete" />
                            </HyperlinkButton>
                        </Grid>
                        <TextBox
                            Header="Street"
                            Text="{x:Bind ViewModel.StreetAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBox
                                Grid.Column="0"
                                Header="City"
                                Text="{x:Bind ViewModel.Locality, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox
                                Grid.Column="1"
                                Header="State / region"
                                Text="{x:Bind ViewModel.Region, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBox
                                Grid.Column="0"
                                Header="Postal code"
                                Text="{x:Bind ViewModel.PostalCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox
                                Grid.Column="1"
                                Header="Country"
                                Text="{x:Bind ViewModel.Country, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Visibility="{x:Bind ViewModel.NoteVisibility, Mode=OneWay}">
                    <StackPanel Spacing="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Text="Note" />
                            <HyperlinkButton
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                Command="{x:Bind ViewModel.RemoveOptionalFieldCommand}"
                                CommandParameter="Note"
                                ToolTipService.ToolTip="Remove note">
                                <SymbolIcon Symbol="Delete" />
                            </HyperlinkButton>
                        </Grid>
                        <TextBox
                            MinHeight="72"
                            AcceptsReturn="True"
                            PlaceholderText="Add contact notes"
                            Text="{x:Bind ViewModel.Note, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <TextBox
                    MinHeight="128"
                    AcceptsReturn="True"
                    Header="Preview"
                    IsReadOnly="True"
                    Text="{x:Bind ViewModel.PreviewVCard, Mode=OneWay}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </ScrollViewer>
</ContentDialog>
