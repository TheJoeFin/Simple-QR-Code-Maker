<ContentDialog
    x:Class="Simple_QR_Code_Maker.Controls.LicensesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Simple_QR_Code_Maker.Models"
    mc:Ignorable="d"
    Title="Open Source Licenses"
    CloseButtonText="Close"
    MinWidth="760">

    <Grid ColumnSpacing="16" Height="480">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Library list  -->
        <ListView
            ItemsSource="{x:Bind ViewModel.Libraries}"
            SelectedItem="{x:Bind ViewModel.SelectedLibrary, Mode=TwoWay}"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:LibraryInfo">
                    <Grid Padding="4,6,0,6" RowSpacing="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{x:Bind Name}" TextWrapping="Wrap" />
                        <TextBlock
                            Grid.Row="1"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{ThemeResource CaptionTextBlockStyle}"
                            Text="{x:Bind LicenseType}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!--  Detail pane  -->
        <Grid Grid.Column="1" RowSpacing="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock
                Style="{ThemeResource SubtitleTextBlockStyle}"
                Text="{x:Bind ViewModel.SelectedLibrary.Name, Mode=OneWay}" />

            <TextBlock
                Grid.Row="1"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.SelectedLibrary.LicenseType, Mode=OneWay}" />

            <HyperlinkButton
                Grid.Row="2"
                Padding="0,0,0,4"
                Content="View on GitHub"
                NavigateUri="{x:Bind ViewModel.SelectedLibrary.GitHubUri, Mode=OneWay}" />

            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <TextBlock
                    FontFamily="Consolas"
                    FontSize="11"
                    IsTextSelectionEnabled="True"
                    Text="{x:Bind ViewModel.SelectedLibrary.LicenseText, Mode=OneWay}"
                    TextWrapping="Wrap" />
            </ScrollViewer>
        </Grid>
    </Grid>
</ContentDialog>
